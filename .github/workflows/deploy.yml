name: deploy
on:
  release:
    types: [published]
jobs:
  deploy:
    if: ${{ !github.event.release.prerelease }}   # RCs are pre-releases → skipped
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.release.tag_name }}
      - name: Deploy (WordPress plugin zip (attached to the GitHub Release))
        run: echo "TODO: deploy to WordPress plugin zip (attached to the GitHub Release). Wired up when the deploy target is provisioned."
